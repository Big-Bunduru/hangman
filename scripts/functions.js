// This is where our functions will go for handling the user's inputs and
// processing the puzzles. From the JSBC, it's suggested that we create a
// Class for Hangman with a constructor function and helper functions. 

class Hangman {
    constructor(arg1,arg2) {

    }

    helperFn1 (arg) {

    }
}